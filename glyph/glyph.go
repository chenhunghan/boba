// Package glyph provides small utilities for rendering Unicode
// glyphs — superscripts, and (in the future) likely subscripts,
// fractions, sparkline cells, and other compact visual primitives
// useful in TUI labels and graphs. Each utility lives in its own
// file (e.g. superscript.go) so the package can grow without
// becoming a single sprawling file.
package glyph
